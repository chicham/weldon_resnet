============
Installation
============

At the command line::

    $ easy_install weldon_resnet

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv weldon_resnet
    $ pip install weldon_resnet
